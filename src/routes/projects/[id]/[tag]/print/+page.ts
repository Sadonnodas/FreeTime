/**
 * A project on paper. Dynamic like the project screen above it, so it cannot
 * be baked at build time: GitHub Pages answers it with the 404.html app shell
 * and the page reads the project from IndexedDB once it is running.
 */
export const prerender = false;
